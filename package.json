{
  "name": "mongo-elastic-connector",
  "version": "1.0.0",
  "description": "A package to dump mongo data in elasticsearch and also sync real time updates between the 2 databases",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dracoonghost",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^7.6.1",
    "mongodb": "^3.5.5"
  }
}
